name: note_clone
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  image_picker: ^1.1.2
  just_audio: ^0.9.42
  path_provider: ^2.1.3
  video_player: ^2.10.0

dependency_overrides:
  record_linux: ^1.0.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
